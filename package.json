{
	"scripts": {
		"dev": "concurrently \"cd backend && pnpm dev\" \"cd frontend && pnpm dev\"",
		"heroku-prebuild": "npm i --prefix backend && npm i --prefix frontend",
		"build": "cd backend && npm run build && cd ../frontend && npm run build"
	}
}
